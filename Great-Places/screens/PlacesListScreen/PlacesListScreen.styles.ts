import {StyleSheet} from "react-native";

export default StyleSheet.create({
    screen: {
        flex: 1,
        width: '100%',
        marginTop: 20,
        // justifyContent: 'center',
        alignItems: 'center',
    },
})
